import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    display: 'flex',
    margin: '.25em',
    marginLeft: '0',
    width: '100%'
  },
  header: {
    margin: '0.5em',
    marginLeft: '0',
  }
});